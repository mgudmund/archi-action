on: [push]

jobs:
  run-archi-cli:
    runs-on: ubuntu-latest
    name: Run Archi CLI
    steps:
      - name: Archi CLI Report
        id: archi-cli
        uses: mgudmund/archi-action@v1.6
        with:
          options: '--help'
      
